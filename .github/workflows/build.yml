name: Build_SkyStream

on: [push]

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4


      # Install dependency Linux yang dibutuhkan JUCE
      - name: Install Linux Dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt update
          sudo apt install -y \
            build-essential \
            libasound2-dev \
            libjack-jackd2-dev \
            libfreetype6-dev \
            libx11-dev \
            libxcomposite-dev \
            libxcursor-dev \
            libxinerama-dev \
            libxext-dev \
            libxrandr-dev \
            libxrender-dev \
            libcurl4-openssl-dev \
            pkg-config \
            freeglut3-dev


      # Configure project dengan CMake
      - name: Configure
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release


      # Compile project
      - name: Build
        run: cmake --build build --config Release --parallel


      # Upload hasil build
      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: SkyStream-TX-${{ matrix.os }}
          path: |
            build/**/*SkyStream_TX*